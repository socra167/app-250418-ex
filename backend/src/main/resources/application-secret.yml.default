spring:
    security:
      oauth2:
        client:
          registration:
            kakao:
              clientId: YOUR_CLIENT_ID

custom:
  prod:
    db:
    password: DB_PASSWORD
  jwt:
    secret-key: YOUR_SECRET_KEY
